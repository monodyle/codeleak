import { CONFIG } from './config.const'

export const pages = {
  home: {
    title: CONFIG.title,
    path: '/',
  },
  login: {
    title: 'Login',
    path: '/login',
  },
}

export const api = {
  shhh: '/api/shhh',
}
